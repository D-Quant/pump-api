import Router from "koa-router";


const router = new Router();
// 获取池子信息 by Raydium Server

router.post('/sell', async (ctx) => {
    let sellAmount: string = ""
    let minSolOutput: string = ""


})

export default router;
